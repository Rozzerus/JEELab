package com.rozzer.model;

import com.rozzer.common.Identity;
import com.rozzer.common.Saved;

/**
 * If Objects have the interface them can be identify and saved in DB
 */
public interface Book extends Identity, Saved {
}
